Apply to: apps/web/\*\*

- Keep route/layout files in src/app
- Keep feature logic in src/modules
- Keep shared concerns in src/shared
- Keep providers in src/providers
- Use typed API wrappers instead of ad-hoc fetch in page files
- Align frontend modules with backend domains
